<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.onoff.electronicjournal.mapper.AdminMapper">
    <select id="getAdmin" resultType="cn.onoff.electronicjournal.model.Admin">
       SELECT * FROM admin  WHERE account=#{a} AND password=#{p}
    </select>
</mapper>